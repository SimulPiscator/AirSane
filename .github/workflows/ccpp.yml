name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: set up directories
      run: mkdir ./build
    - name: configure
      run: cd build && cmake ..
    - name: make
      run: cd build && make
